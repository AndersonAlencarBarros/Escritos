 BufferedReader br = new BufferedReader(new InputStreamReader(System.in));

        int N = Integer.parseInt(br.readLine());
        while (N > 0) {
            conjunto = new ArrayList<>();

            while (N > 0) {
                String entrada = br.readLine();
                conjunto.add(entrada);

                N--;
            }

            System.out.println(conjunto);
            N = Integer.parseInt(br.readLine());


                        int maxS = 0;
                        for (int j = 0; j < menor.length(); ) {
                            for (int i = 1; j + i < menor.length() + 1; i++ ) {
                                System.out.println("j - " + j);
                                System.out.println("i - " + i);
                                System.out.println("maxS - " + maxS);
                                String menorSubstring = menor.substring(j, j + i);
                                System.out.println(menorSubstring);
                                if (maior.contains(menorSubstring)) {
                                    maxS++;
                                } else  {
                                    j += i;
                                    break;
                                }
                            }
                        }